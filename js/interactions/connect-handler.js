/**
 * connect-handler.js - handles port-to-port connection flow (scaffold)
 */
export function initConnectHandler(context) {
  // context: { state, actions, renderer }
  // TODO: implement preview edge, validation, create edge on drop
}
