/**
 * mouse-handler.js - top-level mouse event delegation (scaffold)
 */
export function initMouseHandlers(canvas, handlers) {
  // handlers: { onDown, onMove, onUp }
  // TODO: wire DOM events to handlers
}
