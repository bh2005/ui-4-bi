/**
 * node-renderer.js - renders nodes and ports (scaffold)
 */
export function renderNode(node, ctx) {
  // TODO: draw node rect, label
  // draw ports as small circles on sides
}
export function hitTestNode(node, x, y) {
  // TODO: return true if (x,y) within node bounds
  return false;
}
