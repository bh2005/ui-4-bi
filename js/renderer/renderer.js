/**
 * renderer.js - central render entry (scaffold)
 */
export function render(graphState, ctx) {
  // TODO: call node/edge/grid renderers
  // Example:
  // renderGrid(ctx);
  // graphState.edges.forEach(e => renderEdge(e, ctx));
  // graphState.nodes.forEach(n => renderNode(n, ctx));
}
