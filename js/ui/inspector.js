/**
 * inspector.js - dynamic inspector for selected node/edge (scaffold)
 */
export function createInspector(container) {
  // returns { showFor(node), hide() }
  return {
    showFor: (entity) => { /* TODO */ },
    hide: () => { /* TODO */ },
  };
}
