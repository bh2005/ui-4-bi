2026-04-05  Rule Packs + Dynamische Suchregeln
            Rule Packs (Name, Pack-ID, Contact-Groups):
            state.js: graphState.pack = { id, title, contactGroups }
            index.html: Rule Pack Panel in der linken Sidebar (Pack-ID, Titel, Contact Groups)
            toolbar.js: initPackPanel(), syncPackPanel() - verdrahtet Sidebar-Inputs mit graphState.pack
            toolbar.js: exportCMK() ohne prompt() - nutzt graphState.pack
            toolbar.js: importCMK() uebernimmt packMeta (id/title/contact_groups) nach Import
            toolbar.js: saveGraph() speichert pack mit (Backend + localStorage)
            app.js: pack nach /load und localStorage-Fallback wiederherstellen + syncPackPanel()
            cmk-bi-converter.js: exportToCMK() nutzt graphState.pack als Fallback fuer packId/packTitle
            cmk-bi-converter.js: contact_groups aus graphState.pack.contactGroups im Export
            cmk-bi-converter.js: importFromCMK() gibt packMeta zurueck
            main.py: optionales pack-Feld im Graph-Pydantic-Modell (wird gespeichert/geladen)

            Dynamische Suchregeln (2 neue Node-Typen):
            constants.js: hostregex (Host-Regex, #81C784, search-Icon)
            constants.js: serviceregex (Service-Regex, #B0BEC5, file-search-Icon)
            index.html: Palette-Eintraege fuer hostregex und serviceregex
            node-renderer.js: Regex-Pattern wird auf dem Node als Sub-Label angezeigt
            inspector.js: Regex-Sektion fuer hostregex (Host-Regex) und serviceregex (Host-Regex + Service-Regex)
            inspector.js: _updateRegexOnNode() - aktualisiert Sub-Label live beim Tippen
            cmk-bi-converter.js: hostregex -> host_search + state_of_host (pattern-basiert)
            cmk-bi-converter.js: serviceregex -> service_search + state_of_service (host + service regex)
            cmk-bi-converter.js: importFromCMK erkennt host_search+state_of_host -> hostregex
            cmk-bi-converter.js: importFromCMK erkennt service_search+state_of_service -> serviceregex
            (vs. host_search+call_a_rule -> hostgroup, service_search+call_a_rule -> servicegroup)

2026-04-05  Frontend: /load eingebunden + CI Node.js 24
            app.js: Beim Start zuerst GET /load vom Backend (apiFetch)
              Prioritaet: Backend -> localStorage -> Demo-Graph
            .github/workflows/tests.yml: Node.js 20 -> 24

2026-04-05  Bugfix: 6 failing CI-Tests behoben (Backend)
            main.py: /me und /me/password nutzen current.get("sub") or current.get("username")
              (anonymer User hat keinen "sub"-Key)
            main.py: POST /users gibt 409 statt 400 bei Duplikat-Benutzernamen
            main.py: DELETE /users/{id} gibt 409 statt 400 beim Loeschen des letzten Admins
            main.py: /me/password von POST auf PUT geaendert; old_password-Pruefung hinzugefuegt
            main.py: GET /load Endpoint hinzugefuegt (liest SAVE_FILE zurueck)
            conftest.py: user_store vor main in beiden Fixtures neu laden
              (USERS_FILE-Default-Parameter wurde sonst nicht aktualisiert)
            user_store.py: datetime.utcnow() -> datetime.now(timezone.utc) (Deprecation-Fix)
            Ergebnis: 56/56 Backend-Tests gruen

2026-04-02  GitHub Actions CI (.github/workflows/tests.yml)
            Zwei parallele Jobs: backend (pytest) + frontend (Vitest)
            Trigger: push/PR auf main und develop
            pip-Cache (requirements.txt-Hash) + npm-Cache (package.json-Hash)
            Coverage-Artefakt nach jedem Lauf hochgeladen
            Status-Badges in README.md (OWNER/REPO anpassen)

2026-04-02  Test-Suite (pytest + Vitest)
            tests/backend/conftest.py:       Fixtures (tmp_data, user_store, auth_client, anon_client)
            tests/backend/test_user_store.py: 20 Tests (PBKDF2, CRUD, Duplikat, safe())
            tests/backend/test_auth_manager.py: 9 Tests (JWT, require_auth/admin)
            tests/backend/test_api.py:       22 Tests (login, /me, /users, /save, /validate, cycle)
            tests/frontend/geometry.test.js:  14 Tests (snapToGrid, bezierPoint, getPortPoint)
            tests/frontend/cmk-converter.test.js: 22 Tests (exportToCMK, importFromCMK)
            tests/frontend/auth.test.js:     15 Tests (Token, setSession, apiFetch, 401)
            pytest.ini, vitest.config.js, package.json (Vitest 1.6, jsdom)
            requirements.txt: pytest>=8.0, pytest-asyncio>=0.23

2026-04-02  Docs & Statistik
            statistik.md erstellt: Zeitleiste, LOC pro Modul, Architektur-Baum,
              Token-Schaetzung (~1-2M kombiniert ueber alle Sessions)
            README.md: Verweis auf statistik.md ergaenzt
            FEATURES.md: Grid-Abschnitt + Tests in Roadmap
            handbuch.md: Snap-Grid-Groessen-Selector dokumentiert

2026-04-02  Snap-Grid-Groesse konfigurierbar (10 / 20 / 40 / 80 px)
            state.js: state.snapGrid (Startwert aus localStorage 'bi_snap_grid')
            geometry.js: snapToGrid() nutzt state.snapGrid statt hardcodierter 20
            toolbar.js: setSnapGrid(size) - setzt state, localStorage und CSS-Variable
            index.html: CSS-Variable --snap-grid-px fuer Grid-Hintergrund;
              Select-Dropdown (#snap-grid-size) direkt am Snap-Button

2026-04-02  Bugfix: Waypoints beim Wechsel zu Bezier geleert
            context-menu.js: "Gebogen (Bezier)" loescht jetzt auch edge.waypoints
            Vorher: Auto-Layout-Waypoints (fuer straight routing) blieben auf
            Bezier-Kanten und ergaben fehlerhafte Kurvenpfade.

2026-04-02  Session-Ablauf & Passwort-Änderung
            auth.js: JWT-Expiry-Timer (60s vor Ablauf -> Login-Modal, kein Reload)
            auth.js: Abgelaufenes Token beim Start aus localStorage verworfen
            auth.js: apiFetch 401-Handler ruft direkt showLoginModal() auf
            login.js: showChangePasswordModal() - PUT /me/password
              (nur fuer lokale Benutzer sichtbar, Mindestlaenge 6 Zeichen)
            index.html: Key-Button (btn-change-password) neben User-Badge
            src/struktur.txt entfernt (veraltet)

2026-04-01  Benutzerverwaltung & Authentifizierung
            auth_manager.py: JWT (PyJWT, HS256, 8h), LDAP (ldap3), Checkmk REST-Auth
            user_store.py: lokale Benutzerdatenbank (data/users.json), PBKDF2-HMAC-SHA256
            main.py: Endpoints /login, /me, /auth/config; CRUD /users; /me/password
            Alle Backend-Endpoints mit require_auth / require_admin geschuetzt
            AUTH_ENABLED=false (Standard): kein Login noetig, anonymous-Admin
            Auth-Kette: lokal -> LDAP -> Checkmk
            Letzter-Admin-Schutz beim Loeschen

            Frontend:
            js/core/auth.js: Token-Management (localStorage), apiFetch-Wrapper,
              automatische 401-Behandlung (Session-Ablauf -> Login-Modal)
            js/ui/login.js: Login-Modal (Methoden-Selector lokal/ldap/cmk),
              _updateBadge (Username, Rolle, Admin/Logout-Button)
            js/ui/admin-ui.js: Benutzerverwaltung-Modal (Tabelle, Erstellen,
              Bearbeiten, Aktivieren/Deaktivieren, Loeschen)
            index.html: Admin-Button (Shield), Logout-Button, user-badge-role
            app.js: initLogin() und openAdminModal() integriert
            toolbar.js: saveGraph/validateGraph nutzen apiFetch
            requirements.txt: PyJWT>=2.8, ldap3>=2.9, pydantic>=2.0

2026-04-01  Neue Node-Typen, Themes, Pfeilstile, CMK-Import/Export, Docker
            Node-Typen: Host-Gruppe, Service-Gruppe, Andere BI (mit biRef)
            Dark/Light Theme Toggle (persistent, CSS-Variablen, Sonne/Mond-Button)
            Node-Labels im Light Mode: dunkelgrau (#333333)
            Pfeilstile: Keiner, Chevron, Thin, Dot - in 3 Groessen (Kl/Mi/Gr)
            SVG-Marker mit viewBox-Skalierung (18 Varianten: 3 Stile x 3 Groessen x 2 Zustaende)
            Gerundete Ecken bei orthogonalen Kanten (Quadratic-Bezier, CORNER_R=14px)

            CMK BI Import/Export (js/utils/cmk-bi-converter.js):
            - exportToCMK(): Graph -> vollstaendiges Checkmk BI Pack JSON
            - importFromCMK(): Checkmk BI Pack JSON -> Graph + Auto-Layout
            - Alle Node-Typen gemappt: state_of_host, state_of_service,
              host_search, service_search, call_a_rule
            - Aggregationsfunktionen: worst, best, count_ok
            - Zwei neue Toolbar-Buttons (gruen umrandet): CMK-Import / CMK-Export

            Docker-Setup:
            - Dockerfile (nginx 1.27-alpine + Frontend)
            - Dockerfile.backend (python:3.12-slim + FastAPI/uvicorn)
            - docker-compose.yml mit persistentem data/-Volume
            - docker/nginx.conf mit Reverse-Proxy fuer API-Endpoints
            - .dockerignore, .gitignore
            - main.py: CORS-Middleware, SAVE_FILE aus ENV, data/-Dir auto-erstellt

## 2026-03-31 - Phase 1: Interaktions-Polish

### Issue 4: Connect-Modus verbessern
- Tooltip-Banner erscheint beim Aktivieren des Connect-Modus
- Rechtsklick auf Canvas bricht Connect-Modus sauber ab
- Canvas erhaelt Klasse 'connect-active' fuer CSS-Port-Dot-Sichtbarkeit

### Issue 2: Inspector ausbauen
- Farb-Picker (native color-input) im Node-Inspector, aktualisiert Node-Farbe live
- Autocomplete-Feld fuer Host/Service mit Mock-Daten via HTML datalist
- Routing-Toggle (Gerade/Bezier) direkt im Edge-Inspector
- Layer-Selector im Node-Inspector zur direkten Layer-Zuweisung

### Issue 3: Layers-UI
- Layers-Panel in der linken Sidebar (unter BI Collections)
- Layer hinzufuegen per Plus-Button (Name via Prompt)
- Layer per Auge-Icon ein-/ausblenden
- Layer per Schloss-Icon sperren (gesperrte Nodes nicht verschiebbar)
- Layer loeschen per Hover-Button
- Layer-Zuweisung im Node-Inspector (Select-Feld)

### Issue 1: Ports
- 4 Port-Dots (top/right/bottom/left) an jedem Node
- Port-Dots sichtbar bei Node-Hover oder im Connect-Modus
- Port-Drag: von Port zu Port/Node ziehen erstellt Verbindung
- Gestrichelte Preview-Linie im Edge-SVG waehrend des Drag
- Edge speichert fromPort und toPort
- buildEdgePath nutzt getPortPoint() wenn Ports gesetzt
- Gesperrte Layer blockieren Drag-Start
- Neue Datei: src/js/ui/layers-ui.js


CMK BI Visual Editor – Changelog
=================================

2026-03-31  Refactoring: ES-Modul-Struktur (main.js aufgeteilt)
            - main.js (2298 Zeilen) in 19 ES-Module aufgeteilt
            - Neue Struktur: js/core/, js/renderer/, js/interactions/, js/ui/, js/utils/
            - index.html lädt jetzt <script type="module" src="js/app.js">
            - main.js bleibt als Fallback erhalten (wird nicht mehr geladen)

            Neue Dateien:
            - js/core/constants.js      aggregatorTypes, nodeTypes, LAYOUT, SNAP_GRID
            - js/core/state.js          graphState, history, multiSelect, auditLog, state{}, DOM-Refs
            - js/core/actions.js        undo, redo, pushHistory, selectNode, selectEdge,
                                         toggleMultiSelect, deleteSelected, alignNodes,
                                         enterConnectMode, exitConnectMode, logAudit
            - js/utils/geometry.js      snapToGrid, getNodeSize, getEdgePoint,
                                         getEdgePointToTarget, bezierPoint
            - js/utils/dom-utils.js     escHtml
            - js/renderer/renderer.js   scheduleRedrawEdges (RAF), fullRedraw, applyTransform
            - js/renderer/node-renderer.js  createNodeElement, releaseNodeEl, acquireNodeEl,
                                             startInlineEdit, updateVisibility, updateNodeEl
            - js/renderer/edge-renderer.js  buildEdgePath, redrawEdges, edgeSvg, Waypoint-Drag
            - js/renderer/grid-renderer.js  computeSnaplines, drawSnaplines, clearSnaplines
            - js/interactions/drag-handler.js    makeDraggable
            - js/interactions/connect-handler.js Canvas-Drop (Palette → Canvas)
            - js/interactions/marquee-handler.js Marquee-Selection
            - js/interactions/keyboard-handler.js Delete, Ctrl+Z/Y/S, Escape
            - js/interactions/mouse-handler.js   Scroll-Zoom, Pan, Canvas-Klick
            - js/ui/inspector.js        updateInspector, updateUndoRedoButtons
            - js/ui/toolbar.js          saveGraph, validateGraph, exportJSON, importJSON,
                                         snapAllToGrid, autoLayout, Layout-Dropdown
            - js/ui/context-menu.js     showCtxMenu, openNodeCtxMenu, openEdgeCtxMenu
            - js/ui/audit-ui.js         Audit-Modal, User-Badge, Toast, Preview-Modal
            - js/app.js                 Haupt-Initialisierung, loadDemoGraph

2026-03-31  Fixes & Features (vor Refactoring, in main.js)
            - Zoom: Scroll-Wheel zoomt jetzt zur Mausposition hin (nicht mehr zur Ecke)
            - Default-Linientyp: neue Kanten sind "gerade" (routing: 'straight')
            - getEdgePoint: liest echte DOM-Dimensionen (offsetWidth/offsetHeight)
              statt hardcodierter 140x50 → Connectoren treffen echte Node-Kanten
            - autoLayout TD/LR: orthogonale Waypoints mit Gutter-Routing
              (Kanten gehen durch den Abstand zwischen Layern, nicht durch Nodes)
            - autoLayout Skip-Layer: Bypass-Route außerhalb des Layouts
              (verhindert Durchschneiden von Zwischenlayern)
            - autoLayout: Barycenter-Rückwärtslauf korrigiert (Nachfolger statt Vorgänger)
            - Layout-Button: togglet TD ↔ LR per Hauptklick, Dropdown für GRID
            - Layout-Button: zeigt aktuellen Modus als Label (↓ TD / → LR / ⊞ Grid)
            - Parallele Kanten: min. 2px Abstand (senkrechter Versatz)

2026-03-30  Auto-Layout
            - Sugiyama-inspiriertes Hierarchie-Layout (Top→Bottom, Left→Right)
            - Kreuzungsminimierung mit Barycenter-Heuristik (2 Durchläufe)
            - Zentriertes Layer-Placement
            - GRID-Modus: Alle Nodes am Raster ausrichten
            - Animierte Übergänge (cubic-bezier, 350ms)
            - Split-Button mit Dropdown (TB / LR / GRID)

2026-03-29  Kontextmenü & Edge-Routing
            - Rechtsklick auf Node: Label bearbeiten, Verbinden, Aggregation, Duplizieren, Löschen
            - Rechtsklick auf Kante: Gebogen/Gerade umschalten, Waypoints zurücksetzen,
              Richtung umkehren, Löschen
            - edge.routing = 'straight' → gerade Linie mit L-Waypoints
            - edge.routing = undefined  → Bézier-Kurve mit Kollisionserkennung

2026-03-28  Performance (Phase 3A) + RBAC/Audit (Phase 3B)
            - RAF-Throttling: scheduleRedrawEdges() bündelt Redraws per Animationsframe
            - Viewport-Culling: Nodes außerhalb des Sichtbereichs werden ausgeblendet
            - DOM-Pooling: _nodePool recycelt detached <div>-Elemente
            - Persistent Edge-SVG: <svg> wird gecleared, nicht neu erstellt
            - Audit-Log: in-memory + localStorage (100 Einträge), POST an /audit
            - Audit-Modal: Suche, Filter, CSV-Export
            - User-Badge: Name in localStorage, Popover zum Ändern

2026-03-27  Phase 1 + 2: Multi-Select, Inline-Edit, Align/Distribute,
            Waypoint-Editing, Grid-Snap, Snaplines, Preview-Modal,
            Save/Validate, Import/Export, Undo/Redo
